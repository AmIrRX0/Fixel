{
  "name": "fixel",
  "version": "1.2.0",
  "description": "Turn a labeled GitHub issue into a reviewable pull request with Claude or Codex. Open-source GitHub Action and CLI with fork mode and dry runs.",
  "type": "module",
  "bin": {
    "fixel": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test",
    "benchmark:lessons": "node benchmark/never-fail-twice/run.js",
    "check": "npm test && npm run benchmark:lessons && npm pack --dry-run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.241",
    "@octokit/rest": "^21.1.1"
  }
}
